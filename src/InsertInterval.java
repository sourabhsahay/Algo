public class InsertInterval {
    public int[][] insert(int[][] intervals, int[] newInterval) {

        int[][] finalIntervals = new int[intervals.length + 1][];

        if(newInterval[0] < intervals[0][0])
        {
 
        }
        return null;
    }
}
